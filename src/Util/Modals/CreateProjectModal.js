import Modal from './Modal';
import classes from './Modal.module.css';
const CreateProjectModal = (props) => {

    return (
       <Modal removeBackdrop = {removeBackdrop}>
            
       </Modal>
    )

}

export default CreateProjectModal;